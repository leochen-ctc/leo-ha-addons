# 版本自動化更新 (Version Automation) 實作導覽

我已經在專案中建立了 GitHub Action 工作流，現在您可以讓您的應用程式儲存庫（App Repo）自動更新 Add-on 的版本號了。

## 已完成的變更

### [Component] GitHub Action

#### [UPDATE] [.github/workflows/update-version.yml](file:///f:/Documents/GitHub/leo-ha-addons/.github/workflows/update-version.yml)
- **觸發條件**：支援 `repository_dispatch` (遠端觸發) 與 `workflow_dispatch` (手動測試)。
- **核心邏輯**：接收版本號後，使用 `sed` 修改 [config.yaml](file:///f:/Documents/GitHub/leo-ha-addons/smart-home-butler/config.yaml) 並自動提交推送。

---

## 如何實作自動觸發 (App Repo 端)

要在您的應用程式儲存庫（例如 `smart-home-butler`）中觸發此更新，請按照以下步驟操作：

### 1. 建立 Personal Access Token (PAT)
- 前往 GitHub **Settings -> Developer settings -> Personal access tokens -> Tokens (classic)**。
- 產生一個新 Token，勾選 `repo` 權限。
- **重要**：複製此 Token，稍後會用到。

### 2. 在 App Repo 設定 Secret
- 在 App Repo 的 **Settings -> Secrets and variables -> Actions** 中。
- 新增一個 Repository secret，名稱為 `ADDON_REPO_PAT`，內容填入剛才產生的 Token。

### 3. 修改 App Repo 的 CI 工作流
在 App Repo 的 GitHub Action（例如 `release.yml`）中，加入以下步驟：

```yaml
      - name: Trigger Add-on Repo Update
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.ADDON_REPO_PAT }}" \
            https://api.github.com/repos/leochen-ctc/leo-ha-addons/dispatches \
            -d '{"event_type": "update_version", "client_payload": {"version": "${{ steps.get_version.outputs.VERSION }}"}}'
```
> [!NOTE]
> 請確保 `${{ steps.get_version.outputs.VERSION }}` 是您在該工作流中取得的版本號。

---

## 驗證與測試

### 手動測試 (推薦)
您可以直接在瀏覽器或使用 GitHub CLI 測試 Action 是否運作：
1. 前往 GitHub `leo-ha-addons` 儲存庫的 **Actions** 頁面。
2. 點擊左側的 **Update Add-on Version**。
3. 點擊 **Run workflow** 並輸入一個測試版本號（例如 `1.2.3-test`）。
4. 檢查 [smart-home-butler/config.yaml](file:///f:/Documents/GitHub/leo-ha-addons/smart-home-butler/config.yaml) 是否被成功修改。

### 模擬遠端觸發 (CLI)
如果您有安裝 GitHub CLI (`gh`)，可以在本地執行：
```ps1
gh api repos/leochen-ctc/leo-ha-addons/dispatches -f event_type=update_version -f client_payload[version]=1.0.5
```
